/* Set this to your deployed Worker URL after running `npm run setup`.
   Until it is set, the timeline still works - it falls back to the dates baked
   into the page, and the admin panel will say it can't reach the server. */
window.BT_API = "";
