/* The deployed Worker that serves and stores the dates.
   If this is wrong or unreachable, the timeline still works - it falls back to the
   dates baked into the page, and the admin panel reports it can't reach the server. */
window.BT_API = "https://baby-timeline-api.compmike.workers.dev";
